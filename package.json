{
  "name": "action-get-tag",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --minify"
  },
  "dependencies": {
    "@actions/core": "^3.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4",
    "prettier": "^3.8.3"
  }
}